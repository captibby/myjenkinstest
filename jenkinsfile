pipeline {
    agent any
    parameters {
        string(name: 'envName', defaultValue: 'main', description: 'main,release,prod')
        string(name: 'testType', defaultValue: 'servicetest', description: 'servicetest or e2etest')
    }
    stages {
        stage ('Greetings') {
            steps {
                echo 'Hi, how are you ?'
            }
        }    
        stage ('stage2') {
            steps {
                sh ('pwd \
                    && chmod +x \
                    ./hi.sh')
            }
        }
        stage ('stage3') { 
            steps {
                echo 'weldone'
            }
        }
    }
}
